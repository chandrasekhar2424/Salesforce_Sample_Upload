// trigger for if ifelse triger on Account
@isTest
public class IfIfelseTrigger_Test {
    public static testMethod void ifelseMethod() {
        Account acc = new Account();
        acc.Name = 'Chandra sekhar';
        Insert acc;
    }
}